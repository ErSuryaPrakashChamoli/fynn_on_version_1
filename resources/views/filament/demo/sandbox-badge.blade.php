{{--
    Pinned to the topbar on every sandbox page. A prospect must never be
    able to mistake fabricated numbers for a real book, and putting this
    in a render hook means no individual page can forget it.
--}}
<span class="demo-sandbox-badge" title="All data in this environment is fictional.">
    <span class="demo-sandbox-badge__dot"></span>
    Sandbox data
</span>
